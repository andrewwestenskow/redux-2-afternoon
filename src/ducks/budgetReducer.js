const initialState = {
  purchases: [],
  budgetLimit: null,
  loading: false
}

function budgetReducer(state=initialState, action){
  switch(action.type) {
    default:
      return state
  }
}

export default budgetReducer